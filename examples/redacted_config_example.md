# Redigiertes Konfigurationsbeispiel

```yaml
service:
  name: example_component
  mode: controlled
  logging: structured
  storage: external
  secrets: ENV_ONLY
  runtime_profile: reduced_public_view
```
