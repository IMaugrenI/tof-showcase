# Naming

The public repository family currently contains both underscore and hyphen styles.
That reflects older and newer naming lines.

For new public repositories, prefer hyphenated names.
Existing underscore repositories remain legacy names.
